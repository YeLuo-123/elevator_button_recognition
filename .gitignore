__pycache__/
*.py[cod]
*.cache
.DS_Store
.pytest_cache/
.coverage
.venv/
.venv-*/
.idea/
.vscode/

# Generated outputs and local datasets
runs/
outputs/
datasets/
tmp/
test.mp4
_req_dump.txt

# Model binaries are large. Keep them outside normal Git; use Git LFS if shared.
models/*.pt
models/*.onnx
models/*.engine
models/*.bin
# The trained deployment checkpoint is intentionally shared through Git LFS.
!models/best.pt
!models/button_state_yolo26n_cls.pt

# Training checkpoints must never enter ordinary Git history.
**/weights/epoch*.pt
**/weights/last.pt

# Full offline delivery bundles and local base model
dist/
models/Qwen3-VL-2B-Instruct/
